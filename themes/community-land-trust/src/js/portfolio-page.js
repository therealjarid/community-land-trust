jQuery(document).ready(function($) {
  //@TODO:
  // 1. set 'all' as default
  // 2. REST call to fill grid with all portfolio types
  // 3. on button 'select' event, take in value and filter portfolio types with REST call
});
